import * as React from 'react';
import { LeaderNote } from '@night-engines/ui';

export const Default = () => <LeaderNote tag="Honest promise">If an agent won't pay for itself in your business, you hear that on the first call.</LeaderNote>;
export const WhyFirst = () => <LeaderNote tag="Why this comes first">The brain is also what makes the engine safe: agents act on organized records, not guesses.</LeaderNote>;
