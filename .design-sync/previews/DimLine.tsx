import * as React from 'react';
import { DimLine } from '@night-engines/ui';

export const Default = () => <DimLine>NE-01 · The Brain · half one of two — the data</DimLine>;
export const Short = () => <DimLine>Run log · 2026-09-02</DimLine>;
