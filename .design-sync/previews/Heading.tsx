import * as React from 'react';
import { Heading } from '@night-engines/ui';

export const Page = () => <Heading level={1} kind="page">The brain is the data half.</Heading>;
export const Section = () => <Heading level={2}>What you just watched</Heading>;
export const Plain = () => <Heading level={3} kind="plain">Scheduled, unattended work</Heading>;
