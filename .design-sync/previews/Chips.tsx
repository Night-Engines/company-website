import * as React from 'react';
import { Chips } from '@night-engines/ui';

export const Scope = () => <Chips items={['design → code', 'copy & structure', 'deploy & domain']} />;
export const Capabilities = () => <Chips items={['ingestion', 'cleaning', 'docs → structured records', 'single source of truth', 'search & reporting']} />;
