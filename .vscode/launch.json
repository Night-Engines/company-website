{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Site (Chrome)",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:4180",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "serve"
    },
    {
      "name": "Book page (Chrome)",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:4180/book.html",
      "webRoot": "${workspaceFolder}",
      "preLaunchTask": "serve"
    }
  ]
}
